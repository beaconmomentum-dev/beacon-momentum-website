# Beacon Momentum Authentication Server Configuration

# Server Configuration
NODE_ENV=production
PORT=3000

# Session Secret (CHANGE THIS IN PRODUCTION!)
# Generate a strong random string for production use
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Database
# SQLite database file will be created in server/database/beacon.db

# Security
# Set to true in production to enable HTTPS-only cookies
SECURE_COOKIES=true

# CORS (if needed for API access from different domains)
# CORS_ORIGIN=https://beaconmomentum.com
